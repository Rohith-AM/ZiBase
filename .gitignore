# Dependencies (regenerated by npm install — no need to track)
node_modules/

# Build output (main.js is tracked intentionally for Obsidian releases)
# but sourcemaps are not needed
*.js.map

# OS junk
.DS_Store
Thumbs.db

# Editor config
.vscode/
.idea/

# Env / secrets
.env
.env.local
